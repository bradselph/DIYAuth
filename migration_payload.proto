syntax = "proto3";
option go_package = "./";
package main;

message MigrationPayload {
  repeated OtpParameters otp_parameters = 1;
}

message OtpParameters {
  bytes raw_data = 1;  // Changed to raw bytes to avoid UTF-8 issues
}
